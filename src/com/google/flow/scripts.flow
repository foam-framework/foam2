function fern(t, size) {
  if ( size < 4 ) return;
  t.ss();
  t.fd(size*0.1).rt(3);
  fern(t.lt(80),  size*0.35);
  fern(t.rt(160), size*0.35);
  fern(t.lt(80),  size*0.85);
  t.rs();
}

fern(turtle1.pc('green'), 800)




function tree(t, size) {
  if ( size < 40 ) {
    t.lay(foam.graphics.Circle.create({border: null, color: 'green', alpha: 0.07, radius: 30}));
    return;
  }

  t.ss();
  t.pw(size/20);
  t.fd(size*0.25).bk(7);
  tree(t.rt(20+Math.random()*40), size*0.75);
  tree(t.lt(60+Math.random()*40), size*0.75);
  t.rs();
}

tree(turtle1.pc('black'), 600);


function triangle(t, size) {
  if ( size < 2 ) return;

  t.ss();
  triangle(t, size/2);
  t.fd(size/2);
  triangle(t, size/2);
  t.rt(240).fd(size/2).lt(240);
  triangle(t, size/2);
  t.rt(120).fd(size/2);
  t.rs();
}

// t0, t1, t2
// for each point
//  pick random point
//  go half way

triangle(turtle1.pc('black'), 300);



function triangle(t, size) {
  if ( size < 5 ) return;

  t.ss();
  triangle(t, size/2);
  t.fd(size/2);
  triangle(t, size/2);
  t.fd(size/2).rt(120).fd(size/2);
  triangle(t, size/2);
//  t.fd(size/2).rt(120).fd(size/2);
  t.rs();
}

// t0, t1, t2
// for each point
//  pick random point
//  go half way

triangle(turtle1.pc('black'), 300);
