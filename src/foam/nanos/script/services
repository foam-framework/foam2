p({
  "class":"foam.nanos.boot.NSpec",
  "name":"scriptDAO",
  "serve":true,
  "serviceScript":"""
    mdao = new foam.dao.MDAO(foam.nanos.script.Script.getOwnClassInfo());
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(foam.nanos.script.Script.getOwnClassInfo())
      .setMdao(mdao)
      .setInnerDAO(new net.nanopay.security.HashingJDAO(x, "SHA-256", true, false, mdao, "scripts"))
      .setGuid(true)
      .setPm(true)
      .setDecorator(new foam.nanos.script.ScriptRunnerDAO.Builder(x)
        .setDelegate(new foam.dao.NullDAO(x, foam.nanos.script.Script.getOwnClassInfo()))
        .build())
      .setOrder(new foam.core.PropertyInfo[] {foam.nanos.script.Script.ID})
      .setIndex(new foam.core.PropertyInfo[] {foam.nanos.script.Script.ID})
      .build();
  """,
  "client":"{\"of\":\"foam.nanos.script.Script\"}"
})

p({
  "class":"foam.nanos.boot.NSpec",
  "name":"scriptEventDAO",
  "serve":true,
  "serviceScript":"""
    mdao = new foam.dao.MDAO(foam.nanos.script.ScriptEvent.getOwnClassInfo());
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(foam.nanos.script.ScriptEvent.getOwnClassInfo())
      .setMdao(mdao)
      .setInnerDAO(new net.nanopay.security.HashingJDAO(x, "SHA-256", true, false, mdao, "scriptEvents"))
      .setGuid(true)
      .setFixedSize(new foam.dao.FixedSizeDAO.Builder(x)
        .setComparator(foam.mlang.MLang.DESC(foam.nanos.script.ScriptEvent.LAST_RUN))
        .setSize(100)
        .build())
      .setDecorator(new foam.nanos.script.ScriptEventDAO.Builder(x)
        .setDelegate(new foam.dao.GUIDDAO.Builder(x)
        .setDelegate(new foam.dao.NullDAO(x, foam.nanos.script.ScriptEvent.getOwnClassInfo()))
        .build())
        .build())
      .setOrder(new foam.core.PropertyInfo[] {foam.nanos.script.ScriptEvent.LAST_RUN})
      .setIndex(new foam.core.PropertyInfo[] {foam.nanos.script.ScriptEvent.OWNER})
      .setCluster(false)
      .setRuler(false)
      .build();
  """,
  "client":"{\"of\":\"foam.nanos.script.ScriptEvent\"}"
})
